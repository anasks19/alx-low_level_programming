#include <stdio.h>

/**
 * main - Entry point
 * @argc: argument count
 * @argv: arument vector
 *
 * Return: Always zero (Success).
 */

int main(int argc, char *argv[])
{
printf("%s\n", argv[argc - argc]);
return (0);
}
